import React from 'react';
import Storypart from '../Storypart';

const storypart3 = () => (
  <Storypart
    date={'2012'}
    tags={[
      'Java'
    ]}
    text={(
      <span>
        I keep programming and learning with various (smaller) projects. Unfortunately, my old PC from that time is broken, so I cannot recover my old code (which I would probably cringe about today).
      </span>
    )}
  />
);

export default storypart3;